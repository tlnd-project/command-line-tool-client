## Configuration developer environment

### Install poetry for linux

```bash
$ curl -sSL https://install.python-poetry.org | python3 -
$ poetry --version
```

### Create a Poetry environment and install dependency

```ssh
$ poetry install
```

### Run linter
```ssh
$ flake8
```
